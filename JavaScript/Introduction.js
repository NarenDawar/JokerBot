/*

console.log() = Print something in the console

console = object

.log() = module

Data types: Numbers, Strings, Booleans, Null, Undefined, Symbol, and Object

Arithmetic operators: +, -, *, /, % (modelo, returns the remainder)

// = comments

Use ; after a line of code

Modules to know: 
(x).length
(x).toUpperCase
(x).startsWith('x') = prints "true"
Math.random() * number = random number 0 through number multiplied
Math.floor(x) = rounds to the nearest whole number



*/
